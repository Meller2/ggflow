# ── OS / editor noise ────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Node / SvelteKit ─────────────────────────────────────────────────────────
node_modules
/build
/.svelte-kit
/package
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# ── Secrets ──────────────────────────────────────────────────────────────────
.env
.env.*
!.env.example

# ── Rust / Tauri build artifacts ─────────────────────────────────────────────
/src-tauri/target
/src-tauri/gen/schemas

# Portable runtime / models auto-installed next to the dev binary.
# Covered by /src-tauri/target above; kept explicit as documentation.
/src-tauri/target/debug/runtime
/src-tauri/target/debug/models
/src-tauri/target/release/runtime
/src-tauri/target/release/models

# ── Local-only, never pushed ─────────────────────────────────────────────────
# Marketing site (Next.js) — lives on the maintainer's machine, not in this repo.
/website/

# Agent / MCP tool configs (not part of the app).
/.grok/
/mcps/

# Product planning notes.
/goal.md
/IDEA.md

# Scratch: release prep, screenshots, stray logo copies
# (the logos actually used by the app live in static/).
.tmp-release/
/logo.png
/logo1.png
/photo*.jpg
/photo*.png
/izobrazenie*.png
